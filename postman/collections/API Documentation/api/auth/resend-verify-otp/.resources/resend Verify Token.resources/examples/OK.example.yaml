$kind: http-example
request:
  url: "{{baseUrl}}/api/auth/resend-verify-otp?email=string"
  method: POST
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    email: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
