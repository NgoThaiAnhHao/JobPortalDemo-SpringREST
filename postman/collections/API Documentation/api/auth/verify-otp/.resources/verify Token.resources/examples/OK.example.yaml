$kind: http-example
request:
  url: "{{baseUrl}}/api/auth/verify-otp?token=string"
  method: POST
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    token: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
